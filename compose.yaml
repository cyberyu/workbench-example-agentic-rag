# Create your compose file. To learn more, please visit
# https://docs.docker.com/reference/compose-file/
# NOTE: The local NIM container is no longer used. The chatbot now connects to
# LM Studio running at 192.168.1.188:1234 with model openai/gpt-oss-120b.
# Start LM Studio, load the model, and enable the local server before running the app.
services: {}
      
networks:
  default:
    name: agentic-rag